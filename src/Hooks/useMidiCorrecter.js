import { useState, useEffect, useRef } from "react";

export default function useMidiCorrecter(correctMidiData) {
	function correctMidi(userData) {
		//change the color of incorrect notes to red.
	}

	return {
		correctMidi,
	};
}
